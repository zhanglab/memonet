## This file gives details of each script 

# Scripts in order of use: (RStudio denotes I used on laptop instead of server)
QCthresholds.r: determine QC thresholds based on violin plots of QC metrics

pairwiseOA_clean.r: map memonet data to AIBS data
prediction_score_cutoff_barcodes: generate barcode lists of L2/3 cells based on various prediction score cutoff values
testA_prediction_cutoff.r: test the accuracy of the mapping pipeline on AIBS dataset; map 25% of AIBS data onto remaining 75%, 100x
testA_prediction_cutoff_table.r: combine the 100 prediction_score_*.csv files from testA_prediction_cutoff.r into one file

DESCnormalization: normalize L2/3 counts; RStudio script
DESC_inputFormat.r: subset normalized counts to DGs; RStudio script
DESC_DGclustering.py: clustering using DG-subset genelist

DESeq2_tr_vs_ctrl.r: DE analysis of all glut,GABA, or L2/3 cells
DESeq2_clusterX_vs_others.r: DE analysis of one cluster vs others
DESeq2_clusterX_vs_clusterY.r: DE analysis of one cluster vs a specified reference cluster
DESeq2-table.r: combine DE results into one file

labelTransfer_AO_umapDESC.r: map AIBS L2/3 cells onto our clusters; on unity, it errors at the MapQuery step and doesn't produce the umap images, but all we need is the prediction_scores.csv file for now; will be redoing on andromeda once we decide on a parameter setting



# Unordered scripts, used for generating data for collaborators, testing stuff, etc:
DESC_highRankGenes_step1_loop.py and DESC_highRankGenes_step1_loop15.py: loop through combos of DESC parameters using 7 and 15/22 decimal files, respectively, to see which parameter combo gives best consensus bt 7 and 15 decimal input files
AvgGeneExp.r: calculate avg gene expression per gene per cluster, for EJ
DESeq2_clusterX_vs_clusterY1and2.r: DE analysis of one cluster vs two specified reference clusters (used for n24.L0.7)
labelTransfer_AO_umapDESC_loop.r: run AO mapping on all DESC parameter settings
DESC_highRankGenes_step1_loop15_argparse.py: this was an attempt to set parameters for DESC clustering on command line but took too much time to figure out; prob will delete

